import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    appName: {
        fontSize: 28,
        textAlign: "center",
        fontWeight: "bold",
        color: 'blue',
        marginTop: 25,
        marginBottom: 15,
    },
})

export default styles;